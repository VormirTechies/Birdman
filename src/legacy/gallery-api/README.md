# Legacy Postgres/Supabase gallery API

These files preserve the active route implementations that existed before the
Firestore and Firebase Storage migration. They are outside `src/app`, so they
do not expose HTTP routes.
